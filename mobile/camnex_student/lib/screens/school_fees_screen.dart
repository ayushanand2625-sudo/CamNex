import 'package:flutter/material.dart';

class SchoolFeesScreen extends StatelessWidget {
  const SchoolFeesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("School Fees Screen"),
      ),
    );
  }
}